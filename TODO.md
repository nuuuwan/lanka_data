# TODOs

- In Table visual, add a row number column

- Move the QR Code to the top of the page, next to the input box. Center the whole top content, as a single entity.

- Loading map data is always slow. Find a way to speed it up. Perhaps by caching?
